function setup() 
{
     createCanvas(649, 480);
     canvas.position(110, 250);
     video=createCapture(VIDEO);
     video.hide();
     color="";
}

function draw() 
{
    image(img, 10, 10, 500, 520);
    tint(color);

}
function take_snapshot()
{
    save('filter.png');
    k7v23v3oku.scalacube.pro;

}
